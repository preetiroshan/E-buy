import jwt from 'jsonwebtoken';

//NOTE: COME BACK TO THIS FILE AND TRY TO USE IN USERROUTER FOR SIGNIN BY IMPORTING
const generateToken = (user) => {
  //   return jwt.sign({ email: user.email }, "hello", {
  //     expiresIn: "2h"
  //   })
}

export default generateToken;